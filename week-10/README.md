[↑ Back](../README.md)

# Topic `C3` (Lab #10)

## Topics

Data handling #2 (structures, reading and writing files, processing CSV documents, using functions `qsort()` and `bsearch()`, representing data structures).

## Agenda

1. Implementing sorting exercises using function `realloc()`.
1. Implementing data structures.

## Sample codes

### Implementing sorting exercises using function `realloc()`.

The sample codes solve the EOF version of exercise *Airports* (`P110101`) of the prevous week.

1. [`flights_static_pointers.c`](./samples/flights_static_pointers.c) - Uses a static pointer array to store the records.
1. [`flights_realloc.c`](./samples/flights_realloc.c) - Uses a dynamically allocated array to store the records.
1. [`flights_realloc_pointers.c`](./samples/flights_realloc_pointers.c) - Uses a dynamically allocated pointer array to store the records.

### Implementing data structures.

1. [`linked_list.c`](./samples/linked_list.c) - Implements a linked list.
1. [`binary_search_tree.c`](./samples/binary_search_tree.c) - Implements a binary search tree.
